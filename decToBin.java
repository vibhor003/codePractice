import java.util.*;
public class decToBin {

    public static void main(String[]args){
        int decNo=10;
        int binNo=0;
        int pow=0;
        while(decNo>0){
            int rem = decNo % 10;
            binNo = binNo + (rem * Math.pow());
        }
    }
}
